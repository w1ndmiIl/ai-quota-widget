# 版本记录

## 未发布

- 重构 Electron 主进程：配置、快照持久化、用量缓存和 worker 生命周期拆分为独立模块，`main.js` 只负责应用生命周期与 IPC 编排。
- usage worker 的可见窗口空闲期从 30 秒调整为 75 秒，覆盖一分钟历史刷新周期，避免重复冷启动；禁用 Codex 时不再预热其子进程。
- 模型聚合移入纯函数模块；模型菜单仅在来源、模型、语言或选择变化时重建，高频 resize 和图表指针事件改为逐帧合并。
- 保留现有亮暗配色与玻璃卡片风格，统一所有数据卡片的点击放大、Esc 返回和紧凑模式刷新行为。
- 模型选择器新增方向键、Home/End 和 Esc 导航；设置与重置卡弹窗新增焦点约束、背景隔离和焦点恢复。
- 刷新状态改为可感知的忙碌状态，热力图支持键盘方向键逐日浏览，设置项扩大点击区域并完善焦点反馈。
- 统一右上角操作图标的 16px 尺寸、1.9px 线宽与光学居中；刷新使用单向环形箭头，设置改为更清晰的双滑杆图标。
- 重置卡常态使用与其他额度卡一致的玻璃边框，仅在悬停时显示轻量紫色强调。
- 新增 OpenCode 数据源，通过本机官方 CLI 统计与脱敏会话导出读取 Token 用量。
- 新增 Gemini CLI 数据源，从官方本地会话记录读取 Token、缓存、思考与工具用量。
- 新增 Cline 数据源，支持 VS Code 系列全局存储和新版 Cline CLI 数据目录。
- 模型筛选、历史趋势、累计统计和中英文设置现已覆盖新增 Agent 数据源。
- 补全英文总览、额度标签、模型筛选、趋势图、热力图、状态提示和辅助功能文案。
- 将紧凑窗口收至 `336 × 72`，缩小额度卡片间距及卡片到右侧按钮的留白。
- 更新中英文 README 的真实运行截图和紧凑模式说明。

## v1.3.0 - 2026-07-28

- 新增按模型公开标准文本 API 单价估算 Token 美元价值。
- 首次显示优先读取本地快照，后台用量改为按需扫描，并在托盘空闲时释放工作线程与 Codex 子进程。
- Windows 构建会保留 `release/win-unpacked/.userdata` 中的设置与缓存。
- 清理旧模型用量卡片、未调用函数和重复验证构建产物。
- 修复渲染器在多语言状态初始化前访问文案导致页面中断的问题。

## v1.2.1 - 2026-07-12

- Codex、Claude Code 和 Antigravity 数据源可分别启用。
- 模型选择器支持按来源汇总、展开和筛选。
- 新增可配置的全局快捷键和完整托盘操作。
- 本地用量使用增量扫描和持久缓存，减少重复读取。

## v1.1.0 - 2026-07-12

- 新增 Codex 额度、重置时间和重置卡明细。
- 新增启动快照、紧凑模式和暗色主题。
- 新增 Token 趋势、每日热力图和缓存命中率。

## v1.0.0 - 2026-07-11

- 首次公开发布。
